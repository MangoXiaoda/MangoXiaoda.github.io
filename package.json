{
  "name": "wind-dream-cms-blog",
  "version": "0.1.0",
  "private": true,
  "description": "Hugo Dream blog with Decap CMS admin.",
  "scripts": {
    "dev": "hugo server --bind 127.0.0.1 --baseURL http://127.0.0.1:1313 --port 1313 --disableFastRender",
    "cms": "decap-server",
    "dev:all": "concurrently -n hugo,cms -c green,blue \"npm run dev\" \"npm run cms\"",
    "build": "hugo --minify"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "decap-server": "3.7.0",
    "hugo-extended": "0.161.1"
  }
}
